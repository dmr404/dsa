#include<iostream>
using namespace std;

//1) Given an integer array nums,return true if any value appears at least 
//twice in the array,and return false if every element is distinct.

/*nt distinct(int *arr, int n) {
    for(int i = 0; i<n; i++) {
        for(int j = 0; j<i; j++) {
            if(arr[j]==arr[i]) {
                cout<<"true";
                cout<<arr[j];
            }
        }
    }
    
}

int main() {
    int arr[] = {1,2,3,4,5,6,3,7,8,8};
    int n = sizeof(arr) / sizeof(int);
    distinct(arr, n);

    return 0;
}*/

